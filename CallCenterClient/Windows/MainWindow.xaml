<Window x:Class="CallCenter.Client.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
		<Button Content="LogOut" Command="{Binding Path=LogOutCommand}"/>
        <Button Content="Exit" Command="{Binding Path=CancelCommand}" Grid.Column="1"/>
        <Button Content="Options" Command="{Binding Path=SettingsCommand}" Grid.Column="2"/>
    </Grid>
</Window>
